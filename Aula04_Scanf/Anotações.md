# Scanf

## 1) Pra que serve:
Leitura de dados inseridos pelo teclado.

## 2) Sintaxe:
```c
scanf("%d", &variavel_do_numero);
```

### Regras de formato:
* **%d** -> Número (`int`)
* **%c** -> Caractere (`char`)
* **%f** -> Ponto flutuante (`float` e `double`)